# RepairShop

#Database 
We will use Mysql as our database, It will install and configure it in your machine. To do that we will use docker desktop for windows, 
the scripts to do that there are above, you have to run then in your machine.

